{
  "collectionName": "components_blocks_galleries",
  "info": { "displayName": "Galería de imágenes", "icon": "grid", "description": "Grid de imágenes en 2, 3 o 4 columnas" },
  "options": {},
  "attributes": {
    "images": { "type": "media", "multiple": true, "required": true, "allowedTypes": ["images"] },
    "columns": { "type": "enumeration", "enum": ["2", "3", "4"], "default": "3" },
    "caption": { "type": "string" }
  }
}
